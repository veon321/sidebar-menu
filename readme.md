# Responsive Sidebar Menu with Light/Dark Mode

A sleek, modern, and fully responsive navigation sidebar menu built using clean, semantic HTML5, modern CSS3 (Flexbox, CSS Variables), and vanilla JavaScript. This project adapts perfectly to all screen sizes and remembers the user's theme preference using browser storage.

---

## 📌 About The Project

This project is a code-along recreation built strictly **for educational and learning purposes**. It was developed to practice and master modern UI component structures, smooth CSS transitions, responsive web design (RWD) with media queries, and interactive DOM manipulation.

### 🌟 Key Features:

- **Fully Responsive Design:** Smoothly transitions between desktop views, collapsed states, and a mobile overlay menu navigation.
- **Light / Dark Mode Toggle:** Seamlessly switches themes with an integrated custom toggle-track animation and dynamic icon updating.
- **Theme Persistence:** Saves the user's theme selection in `localStorage` so it persists across page refreshes. It also automatically detects and respects the system's preferred color scheme (`prefers-color-scheme`).
- **Interactive Menu Links:** Dynamic active state handling via JavaScript that shifts the highlight color to the currently selected menu tab.
- **Smart Search Expansion:** Clicking the search icon while the sidebar is collapsed automatically expands the menu and sets the focus on the text input.

---

## 🎓 Credit & Original Author

This project was built as a learning exercise based on the excellent instructional material provided by **Coding Nepal**.

- **Original Video Tutorial:** [Build A Responsive Sidebar Menu in HTML CSS & JavaScript](https://www.youtube.com/watch?v=aWGlFgMQW3U)
- **Source Playlist:** Part of a series focused on modern, practical web UI components.

Recreating this layout helped me deepen my understanding of modular CSS styling, layout structure, and writing cleaner JavaScript event listeners to enhance User Experience (UX).

---

## 🛠️ Built With

- **HTML5** – Semantic structure.
- **CSS3** – Custom properties/variables (`:root`), Flexbox layout, media queries, and smooth animations.
- **JavaScript (ES6+)** – Event handling (`addEventListener`), state toggling via `classList`, and `localStorage` integration.
- **Google Fonts & Material Symbols** – Used for typography ("Poppins") and clean, rounded interface icons.

---
